import React from "react";
import { Breadcrumb as BootBreadcrumb } from "react-bootstrap";

export const Breadcrumb = () => {
    return (
        <BootBreadcrumb>
            <BootBreadcrumb.Item href="/">Home</BootBreadcrumb.Item>
        </BootBreadcrumb>
    );
};
